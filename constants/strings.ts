import { DEFAULT_GROUP_ID } from "~/constants/gitlab";

export default {
  GROUPS: "Groups",
  LOADING: "Loading...",
  INPUT_PLACEHOLDER: `Insert Gitlab group ID, default: ${DEFAULT_GROUP_ID}...`,
  PROJECTS: "Projects",
  USER_COUNT: "User count",
};
